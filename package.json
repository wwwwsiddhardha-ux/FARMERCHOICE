{
  "name": "ai-farmer-fullstack",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "npx kill-port 5000 && cd backend && node server.js"
  }
}
